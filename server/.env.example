# 🔌 DATABASE (Supabase / PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/budgetbuddy?schema=public"

# 🔑 AUTHENTICATION
JWT_SECRET="ganti_dengan_string_random_yang_panjang"

# 📧 EMAIL SERVICE (Optional for Password Reset)
EMAIL_HOST="smtp.mailtrap.io"
EMAIL_PORT=2525
EMAIL_USER="user_anda"
EMAIL_PASS="password_anda"

# 🔔 WEB PUSH (VAPID KEYS)
# Generate via: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY="paste_public_key_disini"
VAPID_PRIVATE_KEY="paste_private_key_disini"
VAPID_SUBJECT="mailto:your-email@domain.com"

# 🌐 SERVER CONFIG
PORT=5000
NODE_ENV="development"